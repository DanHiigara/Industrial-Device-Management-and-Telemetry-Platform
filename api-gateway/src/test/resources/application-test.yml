  spring:
    datasource:
      url: jdbc:h2:mem:api-gateway;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      username: sa
      password:
      driverClassName: org.h2.Driver
    jpa:
      hibernate:
        ddl-auto: create-drop
      open-in-view: false

  logging:
    level:
      root: WARN
  app:
jwt:
  secret: test-secret-test-secret-test-secret-test-secret

