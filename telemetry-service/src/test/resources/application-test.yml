  spring:
    datasource:
      url: jdbc:h2:mem:telemetry-service;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      username: sa
      password:
      driverClassName: org.h2.Driver
    jpa:
      hibernate:
        ddl-auto: create-drop
      open-in-view: false

  logging:
    level:
      root: WARN
  app:
deviceServiceUrl: http://localhost:9999
influx:
  url: http://localhost:8086
  token: test
  org: test
  bucket: test
kafka:
  bootstrap: localhost:9092

